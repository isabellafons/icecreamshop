<h4> Payroll for <%= link_to(@store.name,store_path(@store)) %> for <%= @start_date.to_date.strftime("%-m/%-d/%y") %> to <%= @end_date.to_date.strftime("%-m/%-d/%y") %> </h4> 
<div class="card">
  <div class="card-content">
        <table class="striped highlight">
            <thead>
                <th>Employee</th>
                <th>Hours</th>
                <th>Pay Grade</th>
                <th>Pay Rate</th>
                <th>Pay</th>
            </thead>
            
            <% for pay in @payroll %>
            <tr>
                <td><%= pay.employee_name %></td>
                <td><%= pay.hours %></td>
                <td><%= pay.pay_grade %></td>
                <td><%= pay.pay_rate %></td>
                <td>$<%= pay.pay_earned.to_f.round(2).to_s %></td>
            <% end %>
            </tr>

        </table>
    </div>
</div>